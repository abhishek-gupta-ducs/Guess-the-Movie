<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guess The Movie</title>
    <!-- <link rel="stylesheet" href="../public/styles/index.css"> -->
    <link rel="stylesheet" href="/styles/index.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container px-5 py-5 my-5">
        <h1 class="pb-2 border-bottom title">Guess The Movie</h1>
        <form id="gameForm" action="/startGame" method="POST" class="form-group mt-4">           
            <div class="formContainer">
                <div class="mb-4 feildWidth">
                    <label for="industry" class="form-label">Industry</label>
                    <select id="industry" name="industry" class="form-select p-2 rounded-3 mx-0 shadow w-220px">
                        <option value="any">Any</option>
                        <option value="bollywood">Bollywood</option>
                        <option value="hollywood">Hollywood</option>
                        <option value="both">Both</option>
                    </select>
                </div>
                <div class="mb-4 feildWidth">
                    <label for="fromYear" class="form-label">From Year</label>
                    <select id="fromYear" name="fromYear" class="form-select p-2 rounded-3 mx-0 shadow w-220px">
                        <option value="any">Any</option>
                        <% for (let year = 2000; year <= new Date().getFullYear(); year++) { %>
                            <option value="<%= year %>"><%= year %></option>
                        <% } %>
                    </select>
                </div>
                <div class="mb-4 feildWidth">
                    <label for="toYear" class="form-label">To Year</label>
                    <select id="toYear" name="toYear" class="form-select p-2 rounded-3 mx-0 shadow w-220px">
                        <option value="any">Any</option>
                        <% for (let year = 2000; year <= new Date().getFullYear(); year++) { %>
                            <option value="<%= year %>"><%= year %></option>
                        <% } %>
                    </select>
                </div>
            </div>
            <div class="buttonContainer">
                <button type="submit" class="btn btn-success btn-lg w-100">Start Game</button>
            </div>
        </form>
    </div>
</body>
</html>
